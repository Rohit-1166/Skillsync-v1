# SkillSync — Python Dependencies

# 1. Core NLP & Vector Search (Offline Inference)
# Force CPU-only PyTorch to reduce installation size from 2.5GB to ~150MB 
# (Fixes the 10-20 minute setup time and paging file memory errors on new PCs)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
sentence-transformers>=3.0.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pymupdf>=1.22.0

# 2. API matching endpoints
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6

# 3. Data Analysis & Profiling
pandas>=2.0.0
scikit-learn>=1.2.0

# 4. Utilities & Support (transitive/helper packages)
tqdm>=4.65.0
pyyaml>=6.0.0
python-dotenv>=1.0.0